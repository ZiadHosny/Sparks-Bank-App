import 'package:flutter/material.dart';

const mgBgColor = Color.fromARGB(255, 255, 255, 255);
const mgMainColor = Color(0xFF3F3D56);
const mgOrangeColor = Color(0xFFFF7400);
const mgGreenColor = Color(0xFF3E8E7E);
const mgRedColor = Color(0xFFE92E30);
const mgMenuColor = Color(0xFFC1C1C1);
const mgBlackColor = Color(0xFF101010);

// Gradient color
const mgPrimaryGradient0 = LinearGradient(
  colors: [Color.fromARGB(255, 28, 32, 32), Color.fromARGB(255, 12, 66, 55)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient1 = LinearGradient(
  colors: [Color(0xFFED4264), Color.fromARGB(255, 65, 59, 44)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient2 = LinearGradient(
  colors: [Color(0xFF1A2980), Color.fromARGB(255, 31, 19, 58)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient3 = LinearGradient(
  colors: [Color(0xFF4776E6), Color(0xFF8E54E9)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient4 = LinearGradient(
  colors: [Color(0xFFFF8008), Color(0xFFFFC837)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient5 = LinearGradient(
  colors: [Color(0xFFFF512F), Color(0xFFF09819)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient6 = LinearGradient(
  colors: [Color(0xFFED4264), Color(0xFFFFEDBC)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient7 = LinearGradient(
  colors: [Color(0xFF1A2980), Color(0xFF26D0CE)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient8 = LinearGradient(
  colors: [Color(0xFF4776E6), Color(0xFF8E54E9)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const mgPrimaryGradient9 = LinearGradient(
  colors: [Color(0xFFFF8008), Color(0xFFFFC837)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const double mgDefaultPadding = 20.0;

const List<String> operationIcon = [
  "assets/transfer.svg",
  "assets/withdraw.svg",
  "assets/investigate.svg",
];

const List<String> cardsIcon = ["assets/images/mastercard.svg"];
